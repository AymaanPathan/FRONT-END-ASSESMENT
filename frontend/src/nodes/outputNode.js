import { UniversalNode } from "./UniversalNode";
import { OUTPUT_NODE_CONFIG } from "../nodes_configs/OUTPUT_NODE_CONFIG";
export const OutputNode = () => (
  <UniversalNode nodeConfig={OUTPUT_NODE_CONFIG} />
);
